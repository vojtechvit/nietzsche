CREATE TABLE [nietzschebiography].[thematic_role] (
    [id]   INT           IDENTITY (18, 1) NOT NULL,
    [name] NVARCHAR (50) NOT NULL,
    CONSTRAINT [PK_thematic_role_id] PRIMARY KEY CLUSTERED ([id] ASC)
);

